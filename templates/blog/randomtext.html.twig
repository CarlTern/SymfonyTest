{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

<body>
<h1>Your lucky number is {{ number }} and your word is {{slug}}</h1>
</body>